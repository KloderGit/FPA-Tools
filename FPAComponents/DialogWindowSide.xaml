<Window x:Class="FPAComponents.DialogWindowSide"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FPAComponents"
        mc:Ignorable="d"
        Title="DialogWindowSide" Height="341.912" Width="622.059" ShowInTaskbar="False" WindowStartupLocation="CenterOwner" AllowsTransparency="True" WindowStyle="None">
    <Grid>
        <Border BorderBrush="#FF2F52C9" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="10,0,0,0">
                    <Button x:Name="button" IsDefault="True" Content="Сохранить" Width="120" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <Button x:Name="button1" IsCancel="True" Content="Отмена" Width="120" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <Button x:Name="button2" Content="Очистить" Width="120" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,5,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
